# Reference Check Demo

A company is doing reference checks for drivers. They have an existing form that takes the name and phone number for the driver. But they find out that the phone number is considered PII, and they aren’t able to store it properly, so they need a solution that will ensure their application does not touch the data.
